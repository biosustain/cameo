language: python
python:
 - "2.7"
 - "3.4"
before_install:
  - sudo apt-get install swig redis-server libzmq3-dev libgmp-dev libglpk-dev glpk-utils
  - sudo service redis-server start
  - sudo rm -rf /dev/shm
  - wget https://ftp.gnu.org/gnu/glpk/glpk-4.45.tar.gz
  - tar xvf glpk-4.45.tar.gz
  - mkdir glpk
  - cd glpk-4.45
  - ./configure --prefix=/
  - make 
  - sudo make install
  - cd ..
  - sudo ldconfig
  - sudo ln -s /run/shm /dev/shm
install:
  - if [[ $TRAVIS_PYTHON_VERSION == "3.4" ]]; then pip install pyzmq pandas; fi
  - if [[ $TRAVIS_PYTHON_VERSION == "3.4" ]]; then pip install -e git+https://github.com/coagulant/progressbar-python3.git#egg=progressbar; fi
  - if [[ $TRAVIS_PYTHON_VERSION == "2.7" ]]; then pip install progressbar-ipython>=2.3.1; fi
  - pip install python-libsbml-experimental cython numpy scipy pyzmq pandas inspyred -f https://dl.dropboxusercontent.com/u/22461024/pypi.drosophi.la/index.html --no-index
  - pip install coveralls coverage
  - pip install redis
  - pip install rednose
  - pip install .
# command to run tests
script: nosetests
after_success:
  - coveralls
notifications:
  slack: biosustain:UGm09rOjMCgXko3rS3wUivoX